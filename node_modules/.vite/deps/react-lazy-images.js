import {
  require_react
} from "./chunk-4D5CYJYK.js";
import {
  __commonJS,
  __toESM
} from "./chunk-CEQRFMJQ.js";

// node_modules/invariant/browser.js
var require_browser = __commonJS({
  "node_modules/invariant/browser.js"(exports, module) {
    "use strict";
    var invariant2 = function(condition, format, a2, b, c2, d2, e2, f2) {
      if (true) {
        if (format === void 0) {
          throw new Error("invariant requires an error message argument");
        }
      }
      if (!condition) {
        var error;
        if (format === void 0) {
          error = new Error(
            "Minified exception occurred; use the non-minified dev environment for the full error message and additional helpful warnings."
          );
        } else {
          var args = [a2, b, c2, d2, e2, f2];
          var argIndex = 0;
          error = new Error(
            format.replace(/%s/g, function() {
              return args[argIndex++];
            })
          );
          error.name = "Invariant Violation";
        }
        error.framesToPop = 1;
        throw error;
      }
    };
    module.exports = invariant2;
  }
});

// node_modules/unionize/lib/index.js
var require_lib = __commonJS({
  "node_modules/unionize/lib/index.js"(exports) {
    "use strict";
    var __assign = exports && exports.__assign || function() {
      __assign = Object.assign || function(t) {
        for (var s2, i2 = 1, n2 = arguments.length; i2 < n2; i2++) {
          s2 = arguments[i2];
          for (var p2 in s2)
            if (Object.prototype.hasOwnProperty.call(s2, p2))
              t[p2] = s2[p2];
        }
        return t;
      };
      return __assign.apply(this, arguments);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    function unionize(record, config) {
      var _a = config || {}, _b = _a.value, valProp = _b === void 0 ? void 0 : _b, _c = _a.tag, tagProp = _c === void 0 ? "tag" : _c;
      var creators = {};
      var _loop_1 = function(tag2) {
        creators[tag2] = function(value) {
          var _a2, _b2;
          if (value === void 0) {
            value = {};
          }
          return valProp ? (_a2 = {}, _a2[tagProp] = tag2, _a2[valProp] = value, _a2) : __assign({}, value, (_b2 = {}, _b2[tagProp] = tag2, _b2));
        };
      };
      for (var tag in record) {
        _loop_1(tag);
      }
      var is = {};
      var _loop_2 = function(tag2) {
        is[tag2] = function(variant) {
          return variant[tagProp] === tag2;
        };
      };
      for (var tag in record) {
        _loop_2(tag);
      }
      function evalMatch(variant, cases, defaultCase) {
        if (defaultCase === void 0) {
          defaultCase = cases.default;
        }
        var handler = cases[variant[tagProp]];
        return handler ? handler(valProp ? variant[valProp] : variant) : defaultCase(variant);
      }
      var match = function(first, second) {
        return second ? evalMatch(first, second) : function(variant) {
          return evalMatch(variant, first);
        };
      };
      var identity = function(x) {
        return x;
      };
      var transform = function(first, second) {
        return second ? evalMatch(first, second, identity) : function(variant) {
          return evalMatch(variant, first, identity);
        };
      };
      var as = {};
      var _loop_3 = function(expectedTag2) {
        var _a2;
        as[expectedTag2] = match((_a2 = {}, _a2[expectedTag2] = function(x) {
          return x;
        }, _a2.default = function(val) {
          throw new Error("Attempted to cast " + val[tagProp] + " as " + expectedTag2);
        }, _a2));
      };
      for (var expectedTag in record) {
        _loop_3(expectedTag);
      }
      return Object.assign({
        is,
        as,
        match,
        transform,
        _Record: record
      }, creators);
    }
    exports.unionize = unionize;
    exports.ofType = function() {
      return void 0;
    };
    exports.default = unionize;
  }
});

// node_modules/react-lazy-images/dist/react-lazy-images.es.js
var import_react2 = __toESM(require_react());

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  _extends = Object.assign ? Object.assign.bind() : function(target) {
    for (var i2 = 1; i2 < arguments.length; i2++) {
      var source = arguments[i2];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i2;
  for (i2 = 0; i2 < sourceKeys.length; i2++) {
    key = sourceKeys[i2];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(o2, p2) {
  _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function _setPrototypeOf2(o3, p3) {
    o3.__proto__ = p3;
    return o3;
  };
  return _setPrototypeOf(o2, p2);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf(subClass, superClass);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(self) {
  if (self === void 0) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return self;
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/typeof.js
function _typeof(o2) {
  "@babel/helpers - typeof";
  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o3) {
    return typeof o3;
  } : function(o3) {
    return o3 && "function" == typeof Symbol && o3.constructor === Symbol && o3 !== Symbol.prototype ? "symbol" : typeof o3;
  }, _typeof(o2);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/toPrimitive.js
function toPrimitive(t, r2) {
  if ("object" != _typeof(t) || !t)
    return t;
  var e2 = t[Symbol.toPrimitive];
  if (void 0 !== e2) {
    var i2 = e2.call(t, r2 || "default");
    if ("object" != _typeof(i2))
      return i2;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r2 ? String : Number)(t);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/toPropertyKey.js
function toPropertyKey(t) {
  var i2 = toPrimitive(t, "string");
  return "symbol" == _typeof(i2) ? i2 : String(i2);
}

// node_modules/react-intersection-observer/node_modules/@babel/runtime/helpers/esm/defineProperty.js
function _defineProperty(obj, key, value) {
  key = toPropertyKey(key);
  if (key in obj) {
    Object.defineProperty(obj, key, {
      value,
      enumerable: true,
      configurable: true,
      writable: true
    });
  } else {
    obj[key] = value;
  }
  return obj;
}

// node_modules/react-intersection-observer/dist/react-intersection-observer.esm.js
var import_react = __toESM(require_react());
var import_invariant = __toESM(require_browser());
var INSTANCE_MAP = /* @__PURE__ */ new Map();
var OBSERVER_MAP = /* @__PURE__ */ new Map();
function observe(element, callback, options, rootId) {
  if (options === void 0) {
    options = {};
  }
  (0, import_invariant.default)(!INSTANCE_MAP.has(element), "react-intersection-observer: Trying to observe %s, but it's already being observed by another instance.\nMake sure the `ref` is only used by a single <Observer /> instance.\n\n%s", element);
  if (!options.threshold)
    options.threshold = 0;
  var _options = options, root = _options.root, rootMargin = _options.rootMargin, threshold = _options.threshold;
  if (!element || !callback)
    return;
  var observerId = rootMargin ? threshold.toString() + "_" + rootMargin : threshold.toString();
  if (root) {
    observerId = rootId ? rootId + "_" + observerId : null;
  }
  var observerInstance = observerId ? OBSERVER_MAP.get(observerId) : null;
  if (!observerInstance) {
    observerInstance = new IntersectionObserver(onChange, options);
    if (observerId)
      OBSERVER_MAP.set(observerId, observerInstance);
  }
  var instance = {
    callback,
    visible: false,
    options,
    observerId,
    observer: !observerId ? observerInstance : void 0
  };
  INSTANCE_MAP.set(element, instance);
  observerInstance.observe(element);
  return instance;
}
function unobserve(element) {
  if (!element)
    return;
  var instance = INSTANCE_MAP.get(element);
  if (instance) {
    var observerId = instance.observerId, observer = instance.observer;
    var observerInstance = observerId ? OBSERVER_MAP.get(observerId) : observer;
    if (observerInstance) {
      observerInstance.unobserve(element);
    }
    var itemsLeft = false;
    if (observerId) {
      INSTANCE_MAP.forEach(function(item, key) {
        if (item && item.observerId === observerId && key !== element) {
          itemsLeft = true;
        }
      });
    }
    if (observerInstance && !itemsLeft) {
      observerInstance.disconnect();
      OBSERVER_MAP.delete(observerId);
    }
    INSTANCE_MAP.delete(element);
  }
}
function onChange(changes) {
  changes.forEach(function(intersection) {
    var isIntersecting = intersection.isIntersecting, intersectionRatio = intersection.intersectionRatio, target = intersection.target;
    var instance = INSTANCE_MAP.get(target);
    if (instance && intersectionRatio >= 0) {
      var options = instance.options;
      var _inView = false;
      if (Array.isArray(options.threshold)) {
        _inView = options.threshold.some(function(threshold) {
          return instance.visible ? intersectionRatio > threshold : intersectionRatio >= threshold;
        });
      } else if (options.threshold !== void 0) {
        _inView = instance.visible ? intersectionRatio > options.threshold : intersectionRatio >= options.threshold;
      }
      if (isIntersecting !== void 0) {
        _inView = _inView && isIntersecting;
      }
      instance.visible = _inView;
      instance.callback(_inView, intersectionRatio);
    }
  });
}
var InView = function(_React$Component) {
  _inheritsLoose(InView2, _React$Component);
  function InView2() {
    var _this;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
    _defineProperty(_assertThisInitialized(_assertThisInitialized(_this)), "state", {
      inView: false,
      intersectionRatio: 0
    });
    _defineProperty(_assertThisInitialized(_assertThisInitialized(_this)), "node", null);
    _defineProperty(_assertThisInitialized(_assertThisInitialized(_this)), "handleNode", function(node) {
      if (_this.node)
        unobserve(_this.node);
      _this.node = node;
      _this.observeNode();
    });
    _defineProperty(_assertThisInitialized(_assertThisInitialized(_this)), "handleChange", function(inView, intersectionRatio) {
      _this.setState({
        inView,
        intersectionRatio
      });
      if (_this.props.onChange) {
        _this.props.onChange(inView, intersectionRatio);
      }
    });
    return _this;
  }
  var _proto = InView2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    if (true) {
      if (this.props.hasOwnProperty("render")) {
        console.warn('react-intersection-observer: "render" is deprecated, and should be replaced with "children"', this.node);
      }
      (0, import_invariant.default)(this.node, 'react-intersection-observer: No DOM node found. Make sure you forward "ref" to the root DOM element you want to observe.');
    }
  };
  _proto.componentDidUpdate = function componentDidUpdate(prevProps, prevState) {
    if (prevProps.rootMargin !== this.props.rootMargin || prevProps.root !== this.props.root || prevProps.threshold !== this.props.threshold) {
      unobserve(this.node);
      this.observeNode();
    }
    if (prevState.inView !== this.state.inView) {
      if (this.state.inView && this.props.triggerOnce) {
        unobserve(this.node);
        this.node = null;
      }
    }
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    if (this.node) {
      unobserve(this.node);
      this.node = null;
    }
  };
  _proto.observeNode = function observeNode() {
    if (!this.node)
      return;
    var _this$props = this.props, threshold = _this$props.threshold, root = _this$props.root, rootMargin = _this$props.rootMargin, rootId = _this$props.rootId;
    observe(this.node, this.handleChange, {
      threshold,
      root,
      rootMargin
    }, rootId);
  };
  _proto.render = function render() {
    var _this$props2 = this.props, children = _this$props2.children, render2 = _this$props2.render, tag = _this$props2.tag, triggerOnce = _this$props2.triggerOnce, threshold = _this$props2.threshold, root = _this$props2.root, rootId = _this$props2.rootId, rootMargin = _this$props2.rootMargin, props = _objectWithoutPropertiesLoose(_this$props2, ["children", "render", "tag", "triggerOnce", "threshold", "root", "rootId", "rootMargin"]);
    var _this$state = this.state, inView = _this$state.inView, intersectionRatio = _this$state.intersectionRatio;
    var renderMethod = children || render2;
    if (typeof renderMethod === "function") {
      return renderMethod({
        inView,
        intersectionRatio,
        ref: this.handleNode
      });
    }
    return (0, import_react.createElement)(tag || "div", _extends({
      ref: this.handleNode
    }, props), children);
  };
  return InView2;
}(import_react.Component);
_defineProperty(InView, "defaultProps", {
  threshold: 0,
  triggerOnce: false
});
var react_intersection_observer_esm_default = InView;

// node_modules/react-lazy-images/dist/react-lazy-images.es.js
var import_unionize = __toESM(require_lib());
var o = function(e2, t) {
  return (o = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(e3, t2) {
    e3.__proto__ = t2;
  } || function(e3, t2) {
    for (var n2 in t2)
      t2.hasOwnProperty(n2) && (e3[n2] = t2[n2]);
  })(e2, t);
};
var a;
var i = function() {
  return (i = Object.assign || function(e2) {
    for (var t, n2 = 1, r2 = arguments.length; n2 < r2; n2++)
      for (var o2 in t = arguments[n2])
        Object.prototype.hasOwnProperty.call(t, o2) && (e2[o2] = t[o2]);
    return e2;
  }).apply(this, arguments);
};
function c(e2, t) {
  var n2 = {};
  for (var r2 in e2)
    Object.prototype.hasOwnProperty.call(e2, r2) && t.indexOf(r2) < 0 && (n2[r2] = e2[r2]);
  if (null != e2 && "function" == typeof Object.getOwnPropertySymbols) {
    var o2 = 0;
    for (r2 = Object.getOwnPropertySymbols(e2); o2 < r2.length; o2++)
      t.indexOf(r2[o2]) < 0 && (n2[r2[o2]] = e2[r2[o2]]);
  }
  return n2;
}
!function(e2) {
  e2.NotAsked = "NotAsked", e2.Loading = "Loading", e2.LoadSuccess = "LoadSuccess", e2.LoadError = "LoadError";
}(a || (a = {}));
var u = (0, import_unionize.unionize)({ NotAsked: {}, Buffering: {}, Loading: {}, LoadSuccess: {}, LoadError: (0, import_unionize.ofType)() });
var s = (0, import_unionize.unionize)({ ViewChanged: (0, import_unionize.ofType)(), BufferingEnded: {}, LoadSuccess: {}, LoadError: (0, import_unionize.ofType)() });
var d = function(e2, t) {
  return function(n2) {
    var r2 = g(l(e2, t));
    r2.promise.then(function(e3) {
      return n2.update(s.LoadSuccess({}));
    }).catch(function(e3) {
      e3.isCanceled || n2.update(s.LoadError({ msg: "Failed to load" }));
    }), n2.promiseCache.loading = r2;
  };
};
var f = function(e2) {
  e2.promiseCache.buffering.cancel();
};
var p = function(n2) {
  function r2(e2) {
    var t = n2.call(this, e2) || this;
    return t.promiseCache = {}, t.initialState = u.NotAsked(), t.state = t.initialState, t.update = t.update.bind(t), t;
  }
  return function(e2, t) {
    function n3() {
      this.constructor = e2;
    }
    o(e2, t), e2.prototype = null === t ? Object.create(t) : (n3.prototype = t.prototype, new n3());
  }(r2, n2), r2.reducer = function(e2, t, n3) {
    return s.match(e2, { ViewChanged: function(e3) {
      return true === e3.inView ? n3.src ? u.match(t, { NotAsked: function() {
        return n3.debounceDurationMs ? { nextState: u.Buffering(), cmd: (e4 = n3.debounceDurationMs, function(t2) {
          var n4 = g(m(e4));
          n4.promise.then(function() {
            return t2.update(s.BufferingEnded());
          }).catch(function(e5) {
          }), t2.promiseCache.buffering = n4;
        }) } : { nextState: u.Loading(), cmd: d(n3, n3.experimentalDecode) };
        var e4;
      }, default: function() {
        return { nextState: t };
      } }) : { nextState: u.LoadSuccess() } : u.match(t, { Buffering: function() {
        return { nextState: u.NotAsked(), cmd: f };
      }, default: function() {
        return { nextState: t };
      } });
    }, BufferingEnded: function() {
      return { nextState: u.Loading(), cmd: d(n3, n3.experimentalDecode) };
    }, LoadSuccess: function() {
      return { nextState: u.LoadSuccess() };
    }, LoadError: function(e3) {
      return { nextState: u.LoadError(e3) };
    } });
  }, r2.prototype.update = function(e2) {
    var t = this, n3 = r2.reducer(e2, this.state, this.props), o2 = n3.nextState, a2 = n3.cmd;
    this.props.debugActions && (false, console.log({ action: e2, prevState: this.state, nextState: o2 })), this.setState(o2, function() {
      return a2 && a2(t);
    });
  }, r2.prototype.componentWillUnmount = function() {
    this.promiseCache.loading && this.promiseCache.loading.cancel(), this.promiseCache.buffering && this.promiseCache.buffering.cancel(), this.promiseCache = {};
  }, r2.prototype.render = function() {
    var n3 = this, r3 = this.props, o2 = r3.children, d2 = r3.loadEagerly, f2 = r3.observerProps, p2 = c(r3, ["children", "loadEagerly", "observerProps", "experimentalDecode", "debounceDurationMs", "debugActions"]);
    return d2 ? o2({ imageState: u.LoadSuccess().tag, imageProps: p2 }) : import_react2.default.createElement(react_intersection_observer_esm_default, i({ rootMargin: "50px 0px", threshold: 0.01 }, f2, { onChange: function(e2) {
      return n3.update(s.ViewChanged({ inView: e2 }));
    } }), function(e2) {
      return o2({ imageState: "Buffering" === n3.state.tag ? a.Loading : n3.state.tag, imageProps: p2, ref: e2.ref });
    });
  }, r2.displayName = "LazyImageFull", r2;
}(import_react2.default.Component);
var l = function(e2, t) {
  var n2 = e2.src, r2 = e2.srcSet, o2 = e2.alt, a2 = e2.sizes;
  return void 0 === t && (t = false), new Promise(function(e3, i2) {
    var c2 = new Image();
    if (r2 && (c2.srcset = r2), o2 && (c2.alt = o2), a2 && (c2.sizes = a2), c2.src = n2, t && "decode" in c2)
      return c2.decode().then(function(t2) {
        return e3(t2);
      }).catch(function(e4) {
        return i2(e4);
      });
    c2.onload = e3, c2.onerror = i2;
  });
};
var m = function(e2) {
  return new Promise(function(t) {
    return setTimeout(t, e2);
  });
};
var g = function(e2) {
  var t = false;
  return { promise: new Promise(function(n2, r2) {
    e2.then(function(e3) {
      return t ? r2({ isCanceled: true }) : n2(e3);
    }), e2.catch(function(e3) {
      return r2(t ? { isCanceled: true } : e3);
    });
  }), cancel: function() {
    t = true;
  } };
};
var h = function(t) {
  var n2 = t.actual, r2 = t.placeholder, o2 = t.loading, u2 = t.error, s2 = c(t, ["actual", "placeholder", "loading", "error"]);
  return import_react2.default.createElement(p, i({}, s2), function(e2) {
    var t2 = e2.imageProps, i2 = e2.ref;
    switch (e2.imageState) {
      case a.NotAsked:
        return !!r2 && r2({ imageProps: t2, ref: i2 });
      case a.Loading:
        return o2 ? o2() : !!r2 && r2({ imageProps: t2, ref: i2 });
      case a.LoadSuccess:
        return n2({ imageProps: t2 });
      case a.LoadError:
        return u2 ? u2() : n2({ imageProps: t2 });
    }
  });
};
h.displayName = "LazyImage";
export {
  a as ImageState,
  h as LazyImage,
  p as LazyImageFull
};
//# sourceMappingURL=react-lazy-images.js.map
